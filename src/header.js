import React from 'react';

const Header = () => {
    return (
        <header>
            <img src={logo} alt="logo" />
            <nav></nav>
        </header>
    );
};

export default Header;